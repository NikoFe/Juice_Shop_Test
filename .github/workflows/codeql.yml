name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: javascript-typescript
        # Instructs CodeQL to run the extended maintainability & code-quality rule pack
        queries: security-and-quality